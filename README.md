## html/pug/scss/css/js(ES2017)

__Must create files in `./src`__

### Usage:

`npm i` or `yarn i`

`npm run dev` : watch files and run server
`npm start` : build and run server

`npm test` : include sass-lint / csscomb / jscs / eslint /  
